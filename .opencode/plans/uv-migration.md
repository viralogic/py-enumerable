# Poetry → uv Migration

## Goal
Replace Poetry with uv for dependency management. Use hatchling as build backend.

## Files modified

| # | File | Change |
|---|------|--------|
| 1 | `pyproject.toml` | Rewrite to PEP 621 with `hatchling` build backend. Translate Poetry version specs. |
| 2 | `py_linq/__init__.py` | `__version__ = "1.2.4"` → `"1.4.0"` |
| 3 | `.gitignore` | Add `.venv/` |
| 4 | `AGENTS.md` | Replace all `poetry` commands with `uv` equivalents |
| 5 | `PLANS.md` | Same — update command references |
| 6 | `.github/workflows/ci.yml` | `pip install poetry` → `pip install uv`; `poetry install` → `uv sync`; `poetry run` → `uv run` |
| 7 | `.github/workflows/prod-cd.yml` | Same + `poetry publish --build` → `uv build && uv publish` |
| 8 | `.github/workflows/test-cd.yml` | Same, with dry-run flag on publish |

## Files created
- `.opencode/plans/uv-migration.md` — this file

## Files deleted
- `poetry.lock`

## Files committed
- `uv.lock` (generated by `uv sync`)

## Validation
```bash
rm -rf .venv
uv sync                # creates .venv + uv.lock
uv run flake8 .        # lint
uv run pytest tests    # test
```

## Order of implementation
1. Rewrite `pyproject.toml`
2. Sync version in `__init__.py`
3. Update `.gitignore`
4. Update `AGENTS.md`
5. Update `PLANS.md`
6. Update CI workflows
7. Delete `poetry.lock`
8. `uv sync` + validate
9. Commit
